// console.log("here inside utils")

const name = "abhsihek";
// var fs = require("fs");

const multiply = (a, b) => {
  return a * b;
};

// const getNotes = (title, body) => {
//   const ab = getLoad();
// };

// const getLoad = () => {};

module.exports = multiply;
